# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'GitHub MCP Server Dashboards'
    orgId: 1
    folder: 'GitHub MCP Server'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards